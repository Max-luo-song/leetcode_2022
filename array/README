数组定义：数组是存放在连续内存空间上的相同类型数据的集合
在C++中，二维数组在内存的空间地址是连续的
在Java中，二维数组的内存空间分布是一维指针形式
二分法使用条件：数组有序
二分法一定注意循环不变量，while(left<right)说明使用左闭右开，所以赋值时要使用mid做右半部分。详见704
移除数组只能使用覆盖方法，可以使用双指针法，详见27
对于滑动窗口确定最小满足条件序列是，控制左右。当小的时候右移。使用一个while(sum>=target)进行左移。详见209
对于螺旋矩阵，首先保证循环不变量(左闭右开)。并且注意前后衔接，相邻的横竖链接有相同。详见59
